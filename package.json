{
  "name": "isomorphic-untar",
  "version": "0.0.0-monorepo",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "release": "lerna publish -y from-package --no-git-tag-version --no-push",
    "version": "lerna version",
    "build": "lerna run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "jest": "^26.6.3",
    "lerna": "^4.0.0"
  }
}
